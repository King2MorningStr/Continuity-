name: Build UDAC From ZIP

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Install unzip
      run: sudo apt-get update && sudo apt-get install -y unzip

    - name: Prepare build directory
      run: |
        echo "Creating clean build directory..."
        rm -rf build_tree
        mkdir build_tree

    - name: Unzip build ZIP into the build directory
      run: |
        echo "Unzipping project..."
        unzip -o udac_portal_full_build_ready.zip -d build_tree
      # If your ZIP is inside subfolder, change path above

    - name: Show structure for debugging
      run: |
        echo "===== UNZIPPED STRUCTURE ====="
        tree -L 5 build_tree || ls -R build_tree

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: 17

    - name: Grant gradle wrapper permissions
      run: chmod +x ./build_tree/gradlew

    - name: Build Android App
      working-directory: build_tree
      run: ./gradlew assembleDebug --stacktrace

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: UDAC-APK
        path: build_tree/app/build/outputs/apk/debug/*.apk
